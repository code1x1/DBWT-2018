@model IEnumerable<emensa.Models.Gäste>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Grund)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Ablaufdatum)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FkBenutzerNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Grund)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Ablaufdatum)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FkBenutzerNavigation.EMail)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
